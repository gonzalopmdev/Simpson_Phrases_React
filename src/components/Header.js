import React from "react";
import styled from "styled-components";

export const Header = () => {
  return <Title>Las mejores frases de Los Simpson</Title>;
};

const Title = styled.h1`
  color: #e76f51;
  text-align: center;
`;
